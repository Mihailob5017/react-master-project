import React from 'react';
import CollectionOverview from '../../components/collection-overview/overviewCollection';
const ShopComponent = ({ shopData }) => {
  return (
    <div className="shop-page">
      <CollectionOverview />
    </div>
  );
};

export default ShopComponent;
